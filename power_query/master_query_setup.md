# Power Query setup

Create parameter `pDataFolder` ending with `/sample_data/`. Paste each `.pq` file into a blank query and name it as documented. Replace `sample_data` with district exports for production. All scripts promote headers, trim text, type keys as text, add source file and refresh timestamp, and standardize common Yes/No fields.
