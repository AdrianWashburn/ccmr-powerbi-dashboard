# Model build steps

Load queries, disable load for staging if created, mark DimDate as date table, create relationships, add calculated columns, paste measures, hide technical keys not needed by users, validate counts against CSV row counts, and test RLS roles in Power BI Desktop and Service.
